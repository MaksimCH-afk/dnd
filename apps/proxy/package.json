{
  "name": "@rpg/proxy",
  "version": "0.0.0",
  "private": true,
  "description": "Stateless прокси к OpenRouter (хранит API-ключ, ретраи/фоллбэк, SSE)",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node --import tsx src/server.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@rpg/engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
